import { Story, Meta } from '@storybook/react/types-6-0'
import TabActivity from '.'

export default {
  title: 'TabActivity',
  component: TabActivity
} as Meta

export const Default: Story = () => <TabActivity />
